-- Historical placeholder: this migration was applied directly to the
-- database outside of the tracked migration folder (its original SQL
-- file was never committed). It renamed the "assignments" table to
-- "schedule_assignments". The rename is already reflected in the live
-- database and in the CREATE TABLE statement of
-- 20260625232612_add_schedules_assignments, so this file is a no-op
-- kept only to preserve migration history continuity.
SELECT 1;
